# ModernLMS

See [`PLAN.md`](./PLAN.md).

```bash
cp .env.example .env.local
npm install
npm run dev
```

Student A: `src/lib`, `src/app/api`. Student B: `src/components`, pages.
Use `src/lib/types.ts` and `data/*.json` as the contract.
